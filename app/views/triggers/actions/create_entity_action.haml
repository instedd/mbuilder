Create
.dead-pill {{lookupTableName(action.table)}}
with
.pill{'ng-show' => 'lookupPillStatus(action.pill) == "bound"', draggable: 'true', dragstart: 'bindingDragStart(action.pill)'}
  {{lookupPillName(action.pill)}}
.unbound-pill{'ng-show' => 'lookupPillStatus(action.pill) == "unbound"', draggable: 'true', dragstart: 'bindingDragStart(action.pill)'}
  ???
as
.dead-pill {{lookupFieldName(action.table, action.field)}}
