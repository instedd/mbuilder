.new-sub-action{'ng-show' => 'actionIsSelected(action) || action.actions.length == 0'}
  = render 'shared/triggers/add_action'
.new-sub-action{'ng-hide' => 'actionIsSelected(action) || action.actions.length == 0'}
